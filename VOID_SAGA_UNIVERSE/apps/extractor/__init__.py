# apps/extractor — Markdown Scene Extractor for Void Saga
#
# Reads writer-facing .md prose files and extracts structured scenario data
# for the Narrative Compiler pipeline.
#
# Main entry point: extract_scene(md_path) -> (scenario_dict, report_dict)
